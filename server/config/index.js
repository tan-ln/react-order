const config = {
    port: 666,
    database: {
        DATABASE: 'online_order',
        USERNAME: 'root',
        PASSWORD: '',
        PORT: 3306,
        HOST: 'localhost'
    }
}

module.exports = config